import SwiftUI

struct LogInView: View {
    var body: some View {
        NavigationView {
            VStack {
                Text("Sample Title")
                    .font(.system(size: 50, weight: .heavy))
                
            }
            VStack {
                
            }
        }
    }
}
